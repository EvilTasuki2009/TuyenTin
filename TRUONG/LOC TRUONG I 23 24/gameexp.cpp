#include <bits/stdc++.h>
using namespace std;

int main(){
    freopen("gameexp.inp", "r", stdin);
    freopen("gameexp.out", "w", stdout);

    long sum=0, n;
    cin >> n;
    long a[n];
    for(int i = 0; i < n; i++){
        cin >> a[i];
    }

    for(int i = 0; i < n; i++){
        if()
    }

    return 0;
}
