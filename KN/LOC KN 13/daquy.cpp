#include <bits/stdc++.h>
using namespace std;

int main(){
    freopen("daquy.inp", "r", stdin);
    freopen("daquy.out", "w", stdout);

    long n, k;
    cin >> n >> k;
    long w[n], c[n];
    for(int i = 0; i < n; i++){
        cin >> w[i] >> c[i];
    }


    return 0;
}
