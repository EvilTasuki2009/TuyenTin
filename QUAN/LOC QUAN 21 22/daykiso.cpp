#include <bits/stdc++.h>
using namespace std;

int main(){
    freopen("daykiso.inp", "r", stdin);
    freopen("daykiso.out", "w", stdout);

    string s="";
    long j, greatest = 0;
    cin >> s;

    for(int i = 0; i < s.size(); i++){
        j = 0;
        while(isdigit(a[j])){

            j++;
        }
    }

    return 0;
}
